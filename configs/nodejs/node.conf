# Node.js Development Configuration for QUOCNHO Team

# Node.js version settings
NODE_VERSION=18
NPM_VERSION=latest
YARN_VERSION=stable

# Development server settings
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Vue.js/Vite development settings
VITE_PORT=5173
VITE_HOST=0.0.0.0

# Hot reload settings
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# Memory settings
NODE_OPTIONS=--max-old-space-size=4096

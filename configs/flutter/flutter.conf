# Flutter Development Configuration for QUOCNHO Team

# Flutter SDK settings
FLUTTER_VERSION=stable
DART_VERSION=stable

# Web development settings
FLUTTER_WEB_PORT=5000
FLUTTER_WEB_HOST=0.0.0.0

# Android SDK settings (for mobile development)
ANDROID_SDK_ROOT=/opt/android-sdk
ANDROID_API_LEVEL=33
ANDROID_BUILD_TOOLS_VERSION=33.0.0

# Development optimizations
FLUTTER_WEB_AUTO_DETECT=true
FLUTTER_WEB_CANVASKIT=true

# Hot reload settings
FLUTTER_HOT_RELOAD_ENABLED=true
